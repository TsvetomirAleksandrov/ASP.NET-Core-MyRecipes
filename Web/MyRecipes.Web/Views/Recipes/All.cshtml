@model MyRecipes.Web.ViewModels.Recipes.RecipesListViewModel
@{ this.ViewData["Title"] = "Всички рецепти, страница " + Model.PageNumber; }

<h1>@this.ViewData["Title"]</h1>
<hr />
@*<div class="row">
        @foreach (var recipe in this.Model.Recipes)
        {
    <div class="media col-md-4">
        <img class="mr-3 img-responsive" width="100" src="@recipe.ImageUrl" alt="...">
        <div class="media-body">
            <h5 class="mt-0"> <a asp-action="ById" asp-route-id="@recipe.Id">@recipe.Name</a></h5>
            @recipe.CategoryName
        </div>
    </div>}
    </div>*@


<div class="row">
    @foreach (var recipe in this.Model.Recipes)
    {
<div class="card col-md-4">
    <img class="card-img-top" src="@recipe.ImageUrl" alt="@recipe.Name">
    <div class="card-body">
        <h5 class="card-title">@recipe.Name</h5>
        <p class="card-text">@recipe.CategoryName</p>
        <a asp-controller="Recipes" asp-action="ById" asp-route-id="@recipe.Id" class="btn btn-primary">Отвори</a>
    </div>
</div>}
</div>

<hr />
<partial name="_PagingPartial" model="@Model" />

